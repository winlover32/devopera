<svg xmlns="http://www.w3.org/2000/svg" width="12em" height="12em" preserveAspectRatio="xMidYMid slice" viewBox="0 0 240 240"><title>A simple clock</title><g id="c" transform="translate(120,120)"><g id="h"><line y2="80" stroke="#000" stroke-width="5" opacity=".5"/><animateTransform additive="sum" attributeName="transform" by="360" dur="12h" repeatCount="indefinite" type="rotate"/><circle r="7"/></g><g id="m"><line y2="95" stroke="red" stroke-width="4" opacity=".9"/><animateTransform additive="sum" attributeName="transform" by="360" dur="60min" repeatCount="indefinite" type="rotate"/><circle r="6" fill="red"/></g><g id="s"><line y2="100" stroke="#00f" stroke-width="2"/><animateTransform additive="sum" attributeName="transform" by="360" dur="60s" repeatCount="indefinite" type="rotate"/><circle r="4" fill="#00f"/></g></g><script type="text/ecmascript"><![CDATA[
//  interacts badly with the transforms so don't use it as a function
    var timeNow = new Date();
    document.getElementById("h").setAttribute("transform",'rotate(' +
      (timeNow.getHours()*30 + timeNow.getMinutes() / 2 +180) + ')');
    document.getElementById("m").setAttribute("transform",'rotate(' +
      (timeNow.getMinutes()*6 + timeNow.getSeconds() / 10 +180) + ')');
    document.getElementById("s").setAttribute("transform",'rotate(' +
      (new Date().getSeconds()*6 +180) + ')');
  ]]></script></svg>